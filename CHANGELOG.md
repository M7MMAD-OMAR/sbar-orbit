# Changelog

Versions follow Semantic Versioning. Alpha releases are experimental and may change interfaces without compatibility guarantees.

## Unreleased

### Added

- `bin/sbar-orbit.cmd`, a Windows launcher. `bin/sbar-orbit` is a bash script, so until now there was
  no way to start Orbit on Windows at all: the broker was reachable only by importing `startBroker`
  from another Bun process. It resolves Bun by location rather than from PATH, and refuses by name the
  subcommands that cannot work there (`install`, `update`, `service`, `autostart`, `panel`,
  `settings`, `config`) rather than letting them fail further down.
- `sbar-orbit connector-config --no-launcher`, for an agent host that cannot spawn the launcher
  itself, on every platform. On Windows the configuration names `bin/sbar-orbit.cmd`, which Bun's own
  spawn starts both verbatim and through `cmd.exe`, measured on the guest; a host calling Node's
  `child_process.spawn` without `shell: true` cannot. The Linux launcher is an extensionless bash
  script, which a host can refuse for its own reasons, so the flag is honoured there too rather than
  being advertised in the launcher's help and quietly ignored. The configuration it emits was spawned
  verbatim on the guest and answered `initialize`.
- A managed broker socket path on Windows, `%LOCALAPPDATA%\sbar-orbit\broker.sock`.
  `serviceSocketPath()` used to throw `CONFIG_REQUIRED` there, because it wanted `XDG_RUNTIME_DIR`.

### Fixed

- `hostClassTier()` handed every Windows host the tier the measured guest earned. `Limited` in
  `docs/support-tiers.md` reads "it ran HERE and passed inside a stated limit", and a capability probe
  has run nothing here, so it answers `Reasoned` the way the Fedora 44 branch already does: this host
  class is in reach, and nothing has run on this machine.
- `needsCommand("git")` was applied to two tests that never call git, which is the thing
  `tests/platform-support.ts` warns about in its own first paragraph.

- `doctor --report` said `browserBackendSupported: false` and `browsers: []` on Windows, on a guest
  where a browser launched, answered CDP and rendered a page. Two resolvers answered one question;
  there is one now, `windowsBrowserInstalls()`, and it lives in `runtime-paths.ts` so a capability
  report does not have to load a browser automation library to produce one.
- Starting a session from the person's own browser profile is refused on Windows as a platform, with
  the reason, rather than by accident because no install was detected. App Bound Encryption refuses
  any non default user data directory, so the clone would open signed out.
- A launch timeout reported `Owned Chrome exited with code () => child.exitCode`, on every platform.
  `exitCode` is a function on that interface, so the comparison against `null` was always false and
  the template printed the function's source. The still running case now says how long it waited.
- The record attributing a mid-session browser death was suppressed by the death itself. It was
  guarded on whether a shutdown was under way, and Playwright's `disconnected` handler starts one
  from the same socket EOF the process death produces, so the line almost never reached the journal.
  It is guarded on whether the CALLER asked to stop now.
- `WindowsJob.accounting()` and `processIds()` refuse on a closed handle instead of querying it.
  Windows recycles handle values, so a diagnostic read after the job was closed could succeed against
  an unrelated object and print its counters as the browser's. `close()` takes a final snapshot
  first, which is the figure a caller asking after a dead browser wants anyway.
- Windows browser discovery reported a Chromium install that does not exist. `App Paths` is keyed by
  file name and both Chrome and Chromium ship `chrome.exe`, so a Chrome installed outside the three
  install roots was found twice: once correctly, and once as `chromium` carrying Chrome's binary and
  Chromium's profile directory.
- `hostClassTier()` on Windows stated a failure count that `docs/windows-measured.md`, which the same
  sentence points at, contradicts.
- An owned browser that dies mid session is attributed. Playwright reports `Target page, context or
  browser has been closed`, which names no cause; the failure now carries the browser's exit code,
  its last stderr lines, and on Windows the job object's own counters when the kernel was what reaped
  the tree, and one record goes to the broker's journal at the moment the browser exits.

### Changed

- The guest test suite names which of its suites are Linux only instead of reporting them as
  failures. Measured on the Windows 11 guest across one session: 107 failures, then 76 once the port
  landed, then 48, 39 and 30, with 86 skips each stating a reason at the call site.
  `tests/platform-support.ts` carries the two gates and the rule that governs them. Two of the gates
  probe the host rather than branching on the platform, so a Windows machine with Developer Mode on
  runs the symlink tests for real and a machine with `git` runs the packaging ones.

- Generated agent connector configuration names the launcher and one argument,
  `{"command": "<prefix>/bin/sbar-orbit", "args": ["mcp"]}`, instead of the Bun that ran the
  installer plus an absolute path into a source checkout. The old shape could not survive an upgrade
  or a rollback, because it pointed past the launcher link that `local-install.ts` switches between
  versions, and it could not be right on any machine other than the one that generated it. Hosts
  registered with the previous shape keep working; regenerate with `sbar-orbit connector-config` to
  pick up the new one. Windows still names the interpreter and `src/mcp.ts` directly, because
  `bin/sbar-orbit` is a shell script. `src/connector-entry.ts` is the one place that decides this.
- `src/mcp.ts` falls back to the managed broker's socket when `ORBIT_SOCKET` is unset, the way
  `src/cli.ts` and `src/status.ts` already did, rather than refusing. An adapter started with a
  stripped environment now reaches the same broker. It still refuses when there is no runtime
  directory for that socket to live in.

## 0.1.0-alpha.6 - 14 September 2026

- The one command install on a second machine. On a GitHub Ubuntu 24.04.5 runner with a systemd user session, `./install.sh --json` installed and started `sbar-orbit.service`, `doctor` answered, and a browser session through the installed service navigated to a page, read its heading and captured a frame; the browser half of gate 3 closes on a real machine that is not this workstation. The first attempt found the launcher taking `bun` from the caller's PATH, which a systemd user service does not have, so the service exited 127 on every restart; `bin/sbar-orbit` now finds bun by location and refuses with a named reason when nothing has it, with a test that runs it under a bare PATH. The tab closing test closes its popup on the test's signal rather than a timer a slower host beat. [roadmap](docs/roadmap.md), [validation](docs/validation.md).
- The suite on a second host. `bun run verify` ran inside `sbarorbit.slice` on a GitHub Ubuntu 24.04.5 runner: 233 pass, 0 fail, the 22 native tests skipped, 119 s; the log is attached to the release. Three tests assumed this workstation and now do not: the update test links a launcher of its own, the restore test accepts the `UNSUPPORTED` a filesystem without snapshots answers, and the desktop sampler test skips without `hyprctl`. G28 measured on the Windows runner: a program scoped firewall rule blocks the internet and leaves loopback for headless Chrome with no driver, and holds every `chrome.exe` rather than one session. [support tiers](docs/support-tiers.md).
- Windows, macOS and a real Ubuntu host, measured on borrowed machines. `.github/workflows/platform-probes.yml` runs `experiments/platform-probe/` on GitHub's runners by hand and uploads one report per platform, attached to the `v0.1.0-alpha.5` release. Windows Server 2025: `node:net` serves a named pipe and `Bun.listen` binds `AF_UNIX` while `Bun.serve({unix})` refuses a pipe name (G15); Chrome's ten processes stayed inside a `bun:ffi` job object and died with it (G16); under a 2 GiB commit ceiling and a 25% hard CPU cap headless Chrome rendered a byte identical PNG in the same time as unlimited (G17). macOS 26.6.2: `proc_pid_rusage` from `bun:ffi` with no helper (G24), `launchctl bootout` left no survivors (G25), `cp -c` shares blocks on APFS (G22), a fresh profile writes `v10` (G19), a `gui` job read the profile allowed (G21), a `ditto` copy of Chrome decrypted with no hang (G20, one runner), and cluster residency is not readable on a virtual M1 (G23). Ubuntu 24.04.5: with AppArmor's default userns restriction `bwrap` is refused at the loopback setup, so the egress lease is `in-browser` there and the probe says so (G29); Ubuntu's own sway starts from a private prefix (G5); the bundled runtime is refused by soname (G2). All tier `Limited`. [porting](docs/porting.md), [support tiers](docs/support-tiers.md).
## 0.1.0-alpha.5 - 14 September 2026

- Review fixes for the day's porting work. A misconfigured renderer switch is now a `CONFIG_REQUIRED` refusal when the broker starts, not a per session error handed to the agent, and the managed unit reads `EnvironmentFile=-%h/.config/sbar-orbit/broker.env`, which the installer never rewrites; the native backend reads its environment before it creates anything on disk and removes its directory on any throw before the backend exists; session info carries `compositorPid` beside `renderer`, and `experiments/renderer-cost.ts` measures that process rather than the newest `compositor.json` on the machine, and fails instead of publishing a negative figure when a process it was reading vanishes; `experiments/toolbox-gates.sh` waits as long as the probe can take, finds the scope's process through the unit's cgroup, and carries a G6 failure out of its `tee` pipeline; `smoke.py` keeps the first attempt's refusal in the report when it retries with `--unsupported-gpu`; and the tier and porting headers say which non Fedora rows are container measured.
- The container variant, measured on a Toolbx container. `experiments/toolbox-gates.sh` starts the bundled compositor inside `orbit-gates` and probes it from the host: the Wayland socket on the shared `/tmp` is there and the host's own pointer helper binds it and answers `ready`; Xwayland refuses to start inside, `/tmp/.X11-unix not owned by root or us`, so X11 is refused by the user namespace with no display number allocated and nothing near the person's `:0`; and `systemd-run --user --scope --slice=sbarorbit.slice` from inside registers on the host's user manager, lands the process in the slice by the same cgroup path from both sides, and reads `cpu.stat` from inside. G6 and G7 closed. `smoke.py` gained `--hold` so a display can be probed from outside while it is up. [porting](docs/porting.md).
- The other Linux families, measured as far as a container can. `experiments/linux-families/run.sh` builds Debian 13, Ubuntu 24.04, Arch and openSUSE Tumbleweed images, hands each the runtime this workstation built, and answers four gates from `docs/porting.md` with `experiments/linux-families/smoke.py`, a Python form of the compositor contract probe that needs no Bun. G2: the bundled Fedora wlroots loads and runs on Arch and Tumbleweed and is refused by soname on Debian (`libdisplay-info.so.3`) and Ubuntu (`liblcms2.so.2`, `libdisplay-info.so.3`, and older libinput, libwayland and pixman symbols). G5: each family's own sway, unpacked into a private prefix, starts and passes the probe with the Fedora built pointer helper, Ubuntu's sway 1.9 only with `--unsupported-gpu` beside a proprietary NVIDIA module, which the probe records. G1: no compositor needed a logind session. G29: user namespaces confine a network namespace on all four, with the pid half left to a real host because podman refuses the proc mount. Every result is tier `Limited`; `tests/compositor-smoke.test.ts` keeps the probe honest against this host's runtime. [support tiers](docs/support-tiers.md), [porting](docs/porting.md).
- The GPU renderer, measured and declined. `experiments/renderer-cost.ts` runs the same capture loop on pixman and on GLES2 pinned to the integrated GPU: 2.5 ms of compositor per frame on both at 1280 by 800, 5.4 against 5.0 ms at 1920 by 1200, and the person's Hyprland at 0% through both. Visual Studio Code's Electron binary mapped in about a second on either, so the ten second Electron figure was Docker Desktop's missing bus, not software rendering; G3 and G4 closed. Pixman stays the default. `ORBIT_NATIVE_RENDERER=gles2` with `ORBIT_NATIVE_RENDER_DEVICE` opts a broker in, is refused unpinned or on anything but a render node, and a session's `renderer` field reports what actually bound, read from the compositor's own log. [fedora results](docs/fedora-results.md).
- The GTK file chooser needs no session bus. `experiments/native-editor.ts --dialog` opened GNOME Text Editor's chooser on a private display with no bus, typed a path, opened and saved the file; GTK falls back to its own in process dialog when there is no portal to reach. A sandboxed application's portal dialog would land on the person's desktop and is refused by design. G10 closed. [porting](docs/porting.md).
- Three repeated recovery runs, after the supervisor death fix: 6 tests and 33 assertions each, in 13.17, 12.87 and 12.89 seconds, no failures. [validation](docs/validation.md).
- The viewer follows the browser's languages. It opened in Arabic for everyone, so an English reader saw a right to left page flash before they could switch; it now opens in English unless one of the languages the browser asks for is Arabic, the switch in the rail still outranks that and is remembered per browser, and the markup ships English and left to right so no reader sees the other direction first. `tests/viewer-layout.test.ts` asks for Arabic through `navigator.languages` and checks the English default from the same page with nothing remembered. [viewer design](docs/viewer-design.md).
- The bar's click opens the viewer again, and in Orbit's own window. `orbit-stream --open-viewer`, which the quickshell indicator runs on a click, asked the broker for the link and handed it to `xdg-open`: the person's default browser and their own profile, the one place the viewer must not open, and from a shell whose PATH carries no `xdg-open` it opened nothing and said nothing, which is what the person saw. It now asks the broker to open the viewer with the panel's browser and app window settings, reports which browser took it, and falls back to the desktop launcher only for a broker too old to open it. Measured 14 September 2026 from the person's own desktop: the window opened in Chromium as a window of its own, class `chrome-127.0.0.1__-Default`, since Chromium derives an app window's class from its origin and ignores `--class` there; [preview](docs/preview.md) says so now. The hover popup over the indicator is a tooltip and closes when the pointer leaves it, by design: the click target is the mark.
- G18 measured for one service. The restored real account session from the T6 run was held for 60 minutes, navigating to the protected page every five, and was signed in at all twelve checks; a fresh clone of the person's own profile afterwards still signed in, so `www.npmjs.com` accepted the replayed cookie set and the hold logged nobody out. The gate stays open for a device bound service with a refresh interval shorter than an hour, which this service is not. [porting](docs/porting.md).
- The README says what an agent pays. Measured 14 September 2026 through the broker's socket on a real public page: `session.create` 431 ms, `navigate` 2248 ms of which the page is all, an image observation 87 to 112 ms for a 41 to 58 KiB JPEG, a metadata observation 2 ms, `read` 43 ms, `scroll` 32 ms. The cost is not Orbit's; it is the model turn around each call and each image, so the README now says which loop is cheap, and the viewer's help line no longer says the viewer opens in your browser, since it does not. Four frames from the day's runs sit in `docs/images/` and the README shows three of them.
- Acceptance T6 closed against a real service, and no one typed a password. `experiments/real-account-clone.ts` takes the signed in state from a clone of the person's own Chrome profile, at their request, finds which of four candidate services they are signed in to by where a protected page sends the session rather than by what it says, saves that session's storage state as an account snapshot in a scratch directory that is deleted with the run, and measures the rest: a fresh session on the account name reached the protected page signed in, 9.8 seconds from creation; a third session on the name was refused with `PROFILE_BUSY`; and a fresh clone of the person's profile afterwards still signed in. Measured 14 September 2026 on `www.npmjs.com`, 146 cookies in the snapshot. Nothing about the account is recorded. [accounts](docs/accounts.md).
- `session.create` takes `cloneExtensions: false` to leave a cloned profile's extensions dormant. Found by the real account run: a proxy extension in the person's profile set its own proxy inside the confined clone, whose only route out is the lease's proxy, and every origin then failed as `ERR_INTERNET_DISCONNECTED`. The lease failing closed is the lease working; a session that wanted the logins and not the add-ons had no way past it until now. `cloneOf` itself is written down in [cli](docs/cli.md) for the first time.
- Gate 1 is closed with a figure read on the person's own desktop. `experiments/viewer-cost-desktop.ts` opens the viewer window Orbit opens there, the desktop's default Google Chrome in Orbit's own profile, and reads the viewer's `#cost` line through DevTools for a minute, then closes the window. Measured 14 September 2026 on Hyprland at device pixel ratio 1.5: `Viewer cycle: 75 to 129 ms of every 1000 ms (7 to 13%)`, request 59 to 110 ms, decode 6 to 9 ms, draw 0 ms, on a blank browser session; two to three times what the same viewer reports inside a private display, and the difference is the request rather than the decode or the draw. [validation](docs/validation.md).
- Orbit can update itself now, and the shape of it follows from one fact rather than from what an updater usually looks like: a session is live state inside the broker process, the `Sessions` object, the CDP relay listener and the network namespace below the browser, so a socket handover would move the socket and leave every session behind in the old process. An update can therefore be prepared with no effect on a running session and cannot be activated inside one. Preparing is the automatic half: versions sit side by side under `XDG_DATA_HOME` with `current` and `previous` repointed by rename, so a new version is downloaded, verified, unpacked and given its dependencies with nothing written into the tree the running broker executes from. Activation refuses while any session is open, with no flag to waive it, and a broker that does not answer `doctor` after its restart puts the old link back, restarts again and reports `rolledBack`; the version that failed is kept because it is the evidence. Automatic is off until `update on`, absence of the switch means off, and the switch is read before the feed and everything else so `update off` works with no network and no broker. A version waits 72 hours after publication before this machine will take it, which is the control that would have mattered when a stolen publishing account reached about 6,000 machines in under 40 minutes on another registry while signatures verified every one of them. A different release line is reported and never taken. An install running from a source checkout is refused by name, since its updater is git. Twenty two checks in `tests/update.test.ts` against a fake feed, the three that guard activation run against the code with the guard and the rollback removed where they failed, and `check` run against the real registry. Not measured: an end to end activation with real systemd on a machine whose launcher goes through a managed version, which is the fresh-machine container's job and has not been run. [updates](docs/updates.md).
- Measured what an in place update actually does to a serving broker, because the design turns on it. A session already open survives an overwrite, both a half written file and a completed change of interface, since its supervisor process is already running and it answered `observe` afterwards. What breaks is every new session for as long as the broker and the tree disagree, reported as `BACKEND_FAILED` carrying the Python error rather than as an update in progress. `experiments/live-overwrite.ts`, against a staged copy of the tracked source on a private socket.
- The private display runtime moved out of the source tree. It was `<source>/.runtime/sway`, so a new version in a new directory would have started with none of it and native sessions would have stopped working after an update until somebody rebuilt it. It is one build per pinned package set under `XDG_DATA_HOME`, named after the packages, and a runtime built at the old path is adopted rather than downloaded again. The gate found the defect in the first attempt: `node:fs/promises` `cp` rewrote the unpacked tree's relative soname links into absolute paths back into the source tree, so the adopted runtime worked only while that tree still existed and sway could not load `libliftoff.so.0` the moment it did not. The copy is `cp -a` now. Measured with the source tree's runtime hidden: two native sessions open, capture and drive an application from the shared one.
- Acceptance T6 against a real service has a runner, `experiments/real-account.ts`, that does everything except the one thing only the person may do. It creates a named account session bounded to one origin, navigates to the service, pauses, opens the viewer in Orbit's own window and waits for the person to sign in and press **Save account state**; then it restarts on the saved state, reads the signed in marker through the session's own `read`, and asks a third session to take the lease, which must be refused with `PROFILE_BUSY`. Nothing about the account is recorded. Its dry run reached the waiting step on 14 September 2026; the measured half waits for the person. [accounts](docs/accounts.md).
- Gate G13 is closed, on a screen. `experiments/extension-wake.ts` loads the shipped extension into a headed Chromium 151 inside an Orbit private display, where every surface a browser can show is a toplevel the session's window list counts, and asks what wakes a stopped worker and what each wake draws. Measured 14 September 2026: the worker idled out at 27.1 seconds; the browser starting and a debugger's `ServiceWorker.startWorker` are the only two wakes that need no person, and neither added a window; in two quiet minutes with the worker stopped, nothing ran and nothing appeared. A queued mint cannot reach the worker unbidden, so the person initiated mint the design took in advance is the measured answer, and the toolbar click itself was left to the person, whose gesture it is. With G12 and G14 from the morning, every gate on the extension is closed; what the person's own browser does with it is still unobserved, by rule.
- The viewer window was opened the way the broker opens it, inside a private display, and the cost line read from it. `experiments/viewer-window.ts` spawns the `preview open` command as built, system Chromium on a scratch profile of Orbit's, and reads `#cost` every five seconds for a minute at the default cadence. Measured 14 September 2026: the window mapped in 1345 ms, `display-mode: standalone`, 1280 by 800 of page and nothing else, and the viewer reported `Viewer cycle: 24 to 56 ms of every 1000 ms (2 to 6%)`, request 18 to 50 ms, decode 2 to 3 ms, draw 0 ms, on a blank browser session. Roadmap gate 1 wants the participant's own reading of that line; it is now one window away from them, in [validation](docs/validation.md).
- The viewer no longer opens inside the person's browser. The app window was a window of the person's own browser, on their profile, with their extensions, cookies, history and sign-ins around it, which is the opposite of what a viewer for an agent's work should share, and a window in a browser the person is also using was a tab strip away from becoming a tab. `preview open`, the panel's mark and the desktop launcher now hand the viewer to the chosen browser with a profile that is Orbit's own, `~/.local/state/sbar-orbit/viewer/<browser id>`, made private before the browser is asked to use it: the Chromium family gets `--user-data-dir` on it, `--class=sbar-orbit-viewer` so the compositor groups and remembers the window as Orbit's rather than as another browser window, and the two first-run flags so a fresh profile greets nobody; a Firefox fork gets `--profile` on the same directory and `--new-window`, reasoned from Firefox keying its remoting by profile and not measured, since no Firefox build is installed here. A second open is forwarded to the viewer's own instance, not the person's. Three checks in `tests/host-browsers.test.ts` cover the argv per family, the directory per browser id, and that `openViewer` makes the directory at mode 0700 before spawning. Measured in an Orbit private display with Chromium 151, see [validation](docs/validation.md).
- Fifteen more native applications, one at a time, each mapped. `experiments/application-coverage.ts` launches an application through the broker into a private display, records its first window with title and launch time, keeps one frame, closes it through the session's own window command and moves on. Measured 14 September 2026: 15 of 15 mapped in 714 ms to 2597 ms, across GTK4 with libadwaita, GTK3, LibreOffice's VCL, Qt 6 with KDE Frameworks, a GLFW and OpenGL terminal and one application forced onto Xwayland; launch to first frame cost 21% to 64% of one core, and 163% for kitty on llvmpipe. Two facts for a reader choosing an application are written beside the numbers in [validation](docs/validation.md): Ptyxis maps and then cannot start its shell, because it asks the systemd user bus and a private session has no session bus by design, while Konsole and kitty run one; and KFontView exits 1 silently on a font file, its own refusal rather than the display's. Not measured: anything past the first window.
- A native launch whose window belongs to a grandchild no longer times out. `/usr/bin/libreoffice` is a script whose `oosplash` forks `soffice.bin`, and Writer's window is `soffice.bin`'s, so the wait for a visible window carrying the supervised pid ran its full 30 seconds and reported `DEADLINE_EXCEEDED` for an application that was on screen after four. Measured 14 September 2026 in `experiments/application-coverage.ts`, where Writer was the one entry of fourteen to time out. The supervisor already starts every application in a new session, so every descendant shares its session id, and a mapped window is now matched on that, read from `/proc/<pid>/stat`, with the launched pid still accepted first. Focus then goes to the window that matched rather than to a pid that may own none. One native check in `tests/native-window.test.ts` forks the fixture from a shell with `& wait` so the window is a grandchild's, and it was run first against the unfixed code, where it failed. Writer now maps in 4.1 seconds.
- The mint extension has been loaded and run, for the first time anywhere, and two of its three gates are closed. `experiments/extension-gates.ts` builds the shipped manifest unchanged and loads it into an Orbit owned headless Chromium 151 with a private profile and no session bus, which is a browser and is not the person's, so the rule that keeps agents out of theirs is untouched. The worker registers with its click listener and the `cookies`, `storage` and `runtime` APIs bound, in 113 ms. G12: `chrome.cookies.getAll` returns the `HttpOnly` session cookie beside the plain one, with `httpOnly: true`, the same two names `Network.getCookies` lists; a cookie set with `{topLevelSite, hasCrossSiteAncestor}` comes back with that exact `partitionKey`, is invisible without it, and the returned key is accepted by a second fresh browser and read back there. G14: with nothing attached the worker stops 30.07 seconds after its last work; with a `connectNative` port open to the real `mint_host.py` it is still running at the 150 second cap, and the host process is alive throughout. G13 stays open, because it asks what a wake path puts on the person's screen and a headless browser has none. Three things the run found on the way, each of which cost a run: branded Google Chrome, Orbit's default executable on this host, ignores `--load-extension`, so the first attempt attached to a Chromium component extension's worker and read `feedbackPrivate` as ours; Playwright's CDP client auto attaches to every service worker, so through `launchChrome` the idle worker never stopped at all, and the file speaks CDP by hand with a socket closed between samples for that reason; and on Linux the native messaging host manifest is read from `NativeMessagingHosts` under the browser's user data directory, not `$XDG_CONFIG_HOME`. [porting](docs/porting.md) rows G12 to G14, [packaging](docs/packaging.md), `extension/README.md`.
- Removed the GitHub Actions workflow again, at the owner's decision. It ran the typecheck, the publication audit and the long dash check, all of which take seconds locally and are already required before every commit, and none of which is the suite: that needs cgroup delegation and a private slice, which a hosted runner does not give. A check that cannot run the gate but reports green anyway is worth less than no check. `CONTRIBUTING.md` records both attempts and the reason, so the next person does not add a third.
- The mark in the panel drew at the screen edge and answered neither a hover nor a click, which is how the person found it: two sessions running, a blue bar on the edge, and nothing behind it. Two faults in a row, both about the input region that keeps a collapsed mark from swallowing clicks in the whole card-sized rectangle it keeps laid out beside it. `hideWhenIdle` hides the mark while nothing runs, and the settle that ran while it was hidden wrote an empty region and re-armed nothing, so when work started the mark came back drawn and deaf: the cards only open on a hover, and the hover was what the empty region ate. `render()` now settles again when the mark's visibility changes. That alone was not enough, and the private display is what showed it: the region is written into GDK and reaches the compositor with the next frame the surface commits, and a mark that is not animating commits none, so the corrected region sat unsent until something else happened to repaint. Writing a region now queues a redraw with it. Measured in an Orbit `fedora` session against the person's own settings, edge right, bar style, size 8, `hideWhenIdle` on: before the fix a mark restored from hidden took no pointer at any point across its width, with the region logged at the right rectangle the whole time; after it, hovering the same pixel pulls the card out. Suite 212 passing.

## 0.1.0-alpha.4 - 13 September 2026

- `0.1.0-alpha.4` is on the npm registry and is what `bun add -g sbar-orbit` now resolves to. It was installed back from the registry before this line was written: 95 packages into a temporary Bun home, `--help` and `preflight` running from `global/node_modules/sbar-orbit`, and `install --dry-run --json` reporting `installed: true`. The published tarball is 106 files with the same shasum bun packed locally, carries `bun.lock`, and carries no `__pycache__`, which is the whole point of the version. Two things the registry did that are worth writing down: the first publish returned success while the version was still staged and invisible to `npm view` for about three minutes, and a retry in that window was refused with `Cannot publish over previously staged version` rather than overwriting anything.
- The package that goes to the registry is built by a different path than the source archive, and nothing tested it. `bun pm pack` reads the working tree while the archive is built from the git index, so a directory named in `files` shipped whatever happened to be sitting in it: `0.1.0-alpha.3` carries six `__pycache__` files that were never meant to leave this machine. They are dead weight rather than a leak, since a `.pyc` records its source by relative path and is recompiled the moment the timestamps disagree, which they do after an extract. Two more of the same kind were found with it: `bun.lock` was not shipped at all, so `install --reinstall-deps` could not resolve against the frozen lockfile its own help names, and `install --native` spawned a bootstrap the package does not carry, so it failed with a path bash could not open instead of with a reason. The native step now refuses by name, `native-bootstrap-absent`, and points at the repository rather than at a command an agent may run. `tests/packaging.test.ts` packs the repository and fails on any shipped path git does not track, which is the general form of the `__pycache__` case rather than a list of the three files found today. Both checks were run against the unfixed code first: the tarball listed ten untracked entries, and the refusal read `bash: .../bootstrap.sh: No such file or directory`. Measured from an extracted tarball on this host on 13 September 2026: the frozen lockfile installs 100 packages in 319 ms, and `install --native --dry-run --json` reports the native step failed with the new reason. The published `0.1.0-alpha.3` still carries all three, since a published version is immutable; the next version is where this lands.
- The publication audit read a systemd template unit as an email address, so `user@1000.service` was a finding and the pre-commit hook refused any file that wrote down the unit this project's own installer starts. A rule that cannot name its own service stops the documentation rather than a leak. Unit names are now skipped by their suffix, and an address in the same file is still caught.
- Gate 3 is closed, on a container that is a machine rather than a filesystem. `experiments/fresh-machine/systemd-session.sh` gives a clean Fedora 44 image the half the existing one cannot have: systemd as PID 1, a lingering unprivileged account whose user manager owns delegated `cpu`, `memory` and `pids` controllers, and therefore a real `sbarorbit.slice`. On that machine, from tracked source and the frozen lockfile, the dependency install completes, `preflight` passes, the bootstrap builds the private compositor and the pointer helper, `./install.sh` runs to completion with every step `done`, the broker service comes up and answers `doctor`, a browser session opens, and a native session launches an X11 application, accepts typed text and returns a 1280 by 800 JPEG of it. 1549 ms of processor for the session steps inside 4 CPU, 7946 MiB and 1536 tasks, zero refused forks, and the frame is kept beside the log. Two limits are printed beside it rather than left out: there is no GPU, no compositor and no screen, and the container reads the host's processor and memory totals, so the budget it sized is a quarter of the host. That is `Limited`, not `Measured`. The same script against the commit before the fixes reproduces both failures it found.
- Orbit could not create a session at all on a machine without `btrfs-progs`, and said nothing about why. `Bun.spawn` throws on a binary that is not there rather than returning a failing exit code, so the absence of the tool was not a false out of `createSubvolume`, which is what every caller was written for, but an exception through all of them. Measured 13 September 2026 on a fresh Fedora machine: every `session.create` failed with `ENOENT: posix_spawn \'/usr/bin/btrfs\'`, browser and native alike, on a filesystem that was never going to hold a restore point. A machine with no btrfs now loses restore points and keeps its sessions.
- An unexpected exception in the broker was unattributable, which is what made the above take a day to see rather than a minute. The metadata journal records that a method failed and its code, never the exception, and the line that used to put the cause on the broker's own stderr went with the journal that replaced it, so the message existed nowhere: not in the client response, which is fixed text by design, not in the journal, not in `diagnostics --report`, not on stderr. The cause is on stderr again, beside its trace ID, for exceptions that are not Orbit's own. That is the operator's journal, not anything an agent or a page can read, and the raw text still never reaches a client.
- Split the desktop panel into the three things it was doing in one 2156 line file. `desktop/panel_broker.py` is everything that speaks to Orbit, with no GTK in it: the HTTP over the broker's Unix socket, the status shape the mark renders from, the command socket by which a second invocation hands its request to the first, and the two helpers that shell out. `desktop/panel_draw.py` is the geometry and the paint, the easing curves, the capsule and card outline and the neck between them, and the two widgets that carry them. `desktop/panel.py` keeps the application: the window, where it sits, what it does when the person touches it, and the settings window. Behaviour is unchanged and the content is the same code, moved.
- The half of the panel that talks to Orbit has tests for the first time, in `tests/panel-broker.test.ts`, which is what the split was for: none of it could be reached without a display before. Four checks cover presence being fetched only when something will read it, a second panel handing its request over instead of drawing a second mark, a stale socket from a killed panel being taken over rather than deferred to, and the viewer link being opened only when it carries the loopback address and a token. Verified in a private Orbit display rather than on the person's screen: the mark draws at the edge, hovering pulls the card out on its neck, and `--settings` reaches the running panel through the command socket and opens the window.
- Wrote down what the diagnostic journal costs the person debugging Orbit, which is where the click above spent a day. A `BACKEND_ERROR` reaches its caller as the fixed words `Request failed` and is recorded as a code with no message, so the backend's own sentence survives nowhere: not in the reply, not on disk, and not in the broker's stderr, which stopped carrying it when the journal arrived. Letting the message through is not an option, because a Playwright error quotes the selector it was given and a navigation error quotes the URL, which is page content by another name. So [diagnostics](docs/diagnostics.md) names the route to a cause instead: reproduce against `BrowserBackend` in process, outside the wrapper, which is how `click: Target page, context or browser has been closed` was finally read, and use the trace ID to tie that reproduction back to the recorded event.
- A click that closes its own tab is no longer a backend error. Clicking a popup's Close button, or anything else with `window.close()` behind it, made Playwright reject the gesture that caused the close, because the target it was driving went away mid click, and the agent saw `BACKEND_ERROR` for an action that did exactly what it was asked. Measured on this host before the fix, twenty clicks on a popup that closes itself: three rejections, every one of them `click: Target page, context or browser has been closed`. That is why it had been read as a flaky test rather than a defect. Forgiveness needs two conditions and not one: the page must be gone, and the session must still have a tab to go back to. The second was found by the concurrency test rather than by reasoning, since stopping a session closes every page as well, and a first version that asked only whether the page had closed reported a cancelled action as a successful one. Every other click failure is still raised, because a selector that matches nothing must not read as success, and a closed page is recognised by `isClosed` rather than by matching the message. The reply carries `closed: true` beside `applied: true`, since whether the tab survived is the part Orbit can see and whether the page acted on the gesture never was. The same twenty clicks now fail none, and the full suite is 203 passing. The test is deterministic rather than a repeat of the race: the tab goes while the click is still waiting for its target to become clickable, which reaches the same code path every time, and it was run first against the unfixed backend, where it failed with the code it was written to remove.
- One colour now means one thing. Working was green on the desktop mark and blue on the agent pointer, so the same fact wore two colours depending on where a person happened to look. Working is `#255fce` everywhere it appears: the mark, the pointer, and the wash at the screen edges, whose default stops being the desktop's accent, since a wash laid over a wallpaper nobody chose cannot borrow its contrast from a palette. The word `accent` in `frameColor` still asks for the desktop's own colour, and any value still overrides both. The brand stays the navy `#204c87` and the gap between the two blues is now a rule with a check behind it in [brand](docs/brand.md), because closing it would leave the mark looking lit whenever it is drawn. The glow also got deeper, 28 pixels to 120, with its stops bunched against the edge and thinned out over the rest: a shallow wash can only be seen by being dense, and dense against an edge is a border. Four strips at that depth are about 3.8 MiB of buffer between them on a 2560 by 1440 output, against tens of megabytes for one surface covering the output with a transparent middle. Six checks in `tests/settings.test.ts` pass. Not measured: what the deeper strips cost to paint against the 28 pixel ones, which is the measurement [desktop presence](docs/desktop-presence.md) records for the pulse and not for the depth.
- The viewer gave the screen back the room it was spending on its own controls. The picture's height is now measured from the window and the things above it rather than left to the flow, a `ResizeObserver` on the header, the stage bar, the tab row and the panels recomputes it, and the canvas keeps the captured aspect ratio, so nothing is stretched to fill what is left. Everything secondary went into native disclosures under the stage: display settings, the problem report, typing, and ending the session. The sidebar collapses from a button in the header and remembers the choice in local storage where the browser allows it, starts closed at 900 pixels and under where it opens as an overlay, and closes on Escape. **Focus view** replaces "Make it bigger" and keeps the conversation, the project and the page tabs while it hides the rest; **Fullscreen** uses the browser's own API and enters focus view with it, and says what to use instead when a browser refuses it. None of these touch the session, which is still the difference between them and **Screen size**. What a person is looking at is now named in the browser's own tab as `Conversation · Project | Orbit`, and a conversation strip above the stage stays usable with the sidebar closed. Taking over opens the typing disclosure rather than leaving a person to find it.
- **Report a problem** in the viewer prepares the diagnostic report, and nothing leaves the machine until the person chooses. The panel prepares on demand, opens itself when a viewer error occurs, and while the viewer is open it checks for new agent-tool errors every 15 seconds with preparation limited to once every 30. **Download report** saves the whole retained history as JSON. **Continue to GitHub** opens this project's new-issue page with a title and a compact summary already filled in, capped at 7,000 URL characters by dropping events until it fits, and the panel says in its own words that opening that link shares the summary with GitHub and that the issue will be public. Orbit never opens or submits it. Fourteen checks across `tests/preview.test.ts`, `tests/browser-scroll.test.ts`, `tests/theme.test.ts`, `tests/viewer-layout.test.ts` and `tests/diagnostics-viewer.test.ts` pass, the last two rendering the viewer inside an Orbit session of their own rather than on anybody's screen. Not measured: what the new layout costs per frame against the one it replaces, which is the figure the participant gate still wants. [viewer design](docs/viewer-design.md), [diagnostics](docs/diagnostics.md).
- `0.1.0-alpha.3` is on the npm registry, the first version published anywhere, and it was installed back from the registry into a temporary Bun home before this line was written: 95 packages resolved, and `--help`, `preflight` and `install --dry-run --json` ran from `global/node_modules/sbar-orbit` with the dry run reporting `installed: true`. The registry refused the first attempt for lack of two-factor authentication, which is a registry rule and now a documented one.
- A person can now tell an assistant to stop using Orbit, and the assistant has somewhere to put that. `orbit_usage` and `sbar-orbit usage` read and write one switch per conversation: `off` refuses every later Orbit call in that scope, `on` is only ever set at the person's explicit request, and `status` reads without changing anything. It is an adapter preference and not an authorization boundary, which the class says in its own first line, because the same user's programs can always reach the same sockets. Without `ORBIT_CONVERSATION_ID` the switch lives in the MCP connection and resets when the host reconnects; with it, the CLI and MCP share one file named by the hash of the ID, written to a temporary name and renamed into place so a half written state is never read back. An unreadable state file is a refusal that says how to repair it rather than a silent default to on, and off does not close sessions or cancel work already accepted.
- Observation stopped being one shape. `orbit_observe` takes `mode`: `image`, which now returns the metadata beside the picture rather than a bare image block, so a model that can see the frame also gets the title, tabs, pointer and dimensions in text and in `structuredContent`; and `metadata`, which answers from `session.presence` and captures nothing at all, for the tab and window checks that were paying for a screenshot to read a title. The CLI has the same two, `session observe ID --metadata`, plus `--output PATH`, which writes the decoded bytes to a new file and prints the metadata with the path, so a terminal session keeps base64 out of its context. The output path is opened `wx` at `0o600`, so it never overwrites an existing artifact and never follows a symlink into one. The MCP instructions were rewritten around all of this, and every tool result that is an object now carries `structuredContent` as well as its text. Six checks across `tests/agent-interface.test.ts` and `tests/mcp.test.ts` pass. Not measured: what the metadata mode saves in practice against an image call, on a real host with a real model reading it.
- A session can say which conversation and which project it belongs to. `conversationName` and `projectName` are optional on create, both capped at 80 characters like the names beside them, and the MCP schema tells an agent not to invent a host title it does not have. The CLI reads them from `ORBIT_CONVERSATION_NAME` and `ORBIT_PROJECT_NAME`. They exist because a person watching several agents was being asked to tell sessions apart by an agent name they all share, and they are what the viewer's conversation strip is built from. A CLI that has no `ORBIT_SOCKET` now falls back to the managed service socket instead of refusing, which is what every other entry point already did. The [portable orbit-usage skill](skills/orbit-usage/SKILL.md) carries the short version of the switch and the observation modes for hosts that select skills, and [the agent interface](docs/agent-interface.md) is the reference for the whole surface, with its own limits written down.
- A failed tool call now carries a correlation ID, and everything behind that ID stays on the machine. Every dispatched session operation records a start and an outcome into a private JSONL journal: method, action type, backend, elapsed milliseconds, hashed session and request identifiers, and a generated trace ID returned to the caller as `diagnosticId`. Nothing else crosses that boundary, because a raw exception can carry a page's own text, and the broker no longer prints unexpected errors to its stderr either, so the ID is the whole thread between a failure a person saw and the record of it. The store is treated as hostile on both sides: the directory and its files are `0o700`, opened `O_NOFOLLOW` and refused when they are not a plain single link file, rotated at one megabyte into one previous file, and rebuilt from an allowlist on read rather than parsed and trusted. A write that fails does not break the tool it was recording; it increments a counter the report carries and keeps up to 1,000 events in memory, which are lost on restart and say so. An unexpected backend exit is recorded as its own event. Five checks in `tests/diagnostics.test.ts` pass. Not measured: what the journal costs across a long autonomous run, and whether the retained history is enough to attribute a failure nobody was watching.
- One workspace that will not go no longer ends the sweep. Measured 13 September 2026: a single directory under a path this process could not write to threw out of the loop, so a run with 214 directories and 7.4 GB to reclaim removed none of them and reported only `Command failed`. Each refusal is now carried in its own `refused` list with the reason it gave, and the rest of the sweep continues. The test was run against the unfixed loop first, where it failed with the `EACCES` the fix now records.

## 0.1.0-alpha.3 - 13 September 2026

- The fresh-machine container now builds the native runtime, and found the reason it would not have run anywhere but here. In a clean Fedora 44 image with the build tools present, the bootstrap downloaded, verified and compiled, and the sway it unpacked could not load `libevdev.so.2`: it unpacks three packages rather than installing them, so everything they link against had silently been the workstation's to have. The bootstrap now ends with `ldd` and prints every missing soname with the `dnf` line for the 26 runtime packages resolved from the pinned RPMs' requirements; `preflight` asks the same question of a built runtime and reports it as a native prerequisite with that remedy, tested with a fake loader; and the container's `deps` image carries the packages. Measured 13 September 2026 on the rerun: in the `deps` image the bootstrap exits 0, the compositor it built reports `sway version 1.11`, and the helper is present; in the `minimal` image the bootstrap refuses at the compiler, as it should. What that does not show is a native session on that image, which has no display to open one on.
- `./install.sh --native` builds the private display runtime, so a Fedora machine gets native sessions from tracked source through the same command as everything else. The step is opt-in because it downloads three pinned Fedora packages and compiles the pointer helper, and browser sessions do not need it; it is skipped when the runtime is already there, refuses with the `dnf` line for the C toolchain and the Wayland and xkbcommon development files when those are missing, and reports the bootstrap's own last words when it fails. Measured 13 September 2026 on this Fedora 44 host: the bootstrap, pointed at a scratch directory so the live runtime was untouched, downloaded, unpacked, verified both protocol digests, compiled and reported sway 1.11 in under a minute, 3.3 MB on disk. The step's decisions are unit tested with an injected bootstrap; the `prerequisites` remedy for a missing runtime now names `./install.sh --native` and no longer claims to need elevation, since nothing in the bootstrap does.
- Shaped the package for `bun add -g sbar-orbit`: a `bin` entry, a `files` list that ships the runtime and the Markdown docs and nothing else, Linux and Bun 1.3 declared, `private` dropped. Measured from a `bun pm pack` tarball into a temporary consumer project and a temporary global Bun home: `--help`, `preflight`, `doctor --report` and `install --dry-run --json` all ran from `node_modules/sbar-orbit` and planned the same steps as a checkout. Nothing has been published, so the registry path is tier `Limited` and [packaging](docs/packaging.md) says exactly what was and was not shown. The fresh-machine container run was repeated against the new budget code and kept its documented shape: extract, dependency install and `doctor` pass, and both install paths refuse a host with no systemd user session by their intended messages.
- Removed every non-null assertion from the source, the way the conventions ask: a destructured first element with its own refusal in place of `argv[0]!`, typed fields for the compositor's socket and display in place of reading them back out of the environment, an `input()` accessor that names a missing pointer helper in place of `this.device!`, and `charAt`, `??` and a typed lookup table for the rest. 39 tests across the touched files pass.
- Sized the shared budget to the machine, and named the three walls it was hitting. Measured 13 September 2026 with three browser sessions open for another agent on a 24 thread, 31 GiB host: the slice stood at 453 of its 512 tasks, about 150 per settled session, and a fourth session failed inside 407 ms with a refused fork that surfaced as a browser that never published its endpoint, `pids.events max` rising by ten across one test run; with the task cap out of the way the fixed one core quota throttled the slice in 18,252 of 49,195 scheduling periods, 37%, and every further Chrome start timed out at its 20 second handshake; and with that out of the way the fixed 2 GiB sat pinned at its 1792 MiB pressure threshold with 1,569,927 `memory.high` reclaim events, inside which a browser asked for a new page hung or lost its renderer. The budget is now a quarter of the machine on each axis, floored at one core and 2 GiB and capped at four cores and 8 GiB, so a four core laptop keeps exactly the limits it had; tasks are 1536, which protects the desktop from nothing the other two do not. Weight and niceness, which are what keep the desktop responsive, are unchanged. The full suite then ran in 54 seconds against 88 to 104 before, 196 passing, with zero memory events, zero refused forks and 43 of 544 periods throttled during the run, while the other agent's sessions stayed open throughout.
- A browser session now refuses before its fork rather than after. `launchChrome` reads the slice's live counters and raises `RESOURCE_EXHAUSTED` naming the free tasks and megabytes when there is less room than a launch needs, which was measured by sampling `pids.current` every 10 ms through a start: 132 tasks at the peak of the fork burst, 139 when the endpoint is published, 150 settled. The failure message also carries Chrome's own last words now: its stderr was discarded, which is how a refused fork spent a day reported as a browser that did not publish its endpoint.
- The managed broker sweeps abandoned workspaces when it starts. `~/.cache/sbar-orbit/workspaces` held 417 directories and 7.2 GB from brokers that had been stopped or killed, because the only thing that reclaimed them was `sbar-orbit clean`, which nobody ran. A directory whose recorded owner is the fixed managed socket but another pid was written by a broker that is gone, and goes; a private broker started by a test keeps its own.
- The viewer decodes each frame with the engine's base64 decoder where it exists, `Uint8Array.fromBase64`, rather than a JavaScript callback per byte. Measured in the owned Chrome on a 67 KB frame over 60 decodes: median 6.2 ms to 3.8 ms, p90 8.3 ms to 4.3 ms. A `data:` fetch would have done the same and the page's `connect-src` forbids it, which the preview test found. The freshness readout also writes a node only when its text changed, and `status --watch` polls at the panel's cadence, one second while an agent acts and up to five when nothing answers, instead of a fixed second.
- The MCP server reads its version from `package.json` instead of carrying a copy that a release would have to remember. The launcher's help now lists every verb the CLI accepts; `preview open`, `preview browsers`, `session observe`, `pause`, `resume`, `journal`, `restore` and `account save` all worked and none was written down. A GitHub workflow runs the typecheck, the public audit and the long dash rule on every push, which is what a hosted runner can check; the suite itself needs a delegated cgroup and stays local.
- Removed the website's deployment workflow, and with it the reason to keep a Cloudflare API token in the repository's secrets. It had never deployed anything: both of its runs failed on the missing secret, while the site that is live was put there by a person running `wrangler deploy` from a machine that is already logged in. Deployment is now that command and nothing else, so a push to `main` deploys nothing and no credential lives in the repository. The site stays where it is, on Workers with the custom domain declared in `website/wrangler.jsonc`.

## 0.1.0-alpha.2 - 13 September 2026

- Fixed the release smoke test asking the packaged browser for a format it stopped producing. Frames became JPEG on both backends, and this check still read the bytes for a PNG signature, so the first packaging run of this release failed on a capture that had worked. It now reads the format the frame declares and checks that frame's own magic bytes, which is the rule `tests/frame-format.ts` already holds the suite to. Caught by running the documented release commands rather than by reading them.

- Took the lines and the colours out of the viewer, and the project's vocabulary with them. Nothing draws a border any more, including the hairline rings that were borders wearing a shadow's name, so separation is carried by the surface a thing sits on and the shadow it casts; the surfaces had to move about eight per cent apart to do alone what a line was helping with. The palette is white, black and the greys between, which means state can no longer be a green light and an amber one. It is brightness, motion and words instead: bright and moving is working, bright and still needs you, dim is open, dimmest is done. That also survives being read by somebody who cannot separate red from green, which the light it replaces did not. `prefers-reduced-motion` removes every animation, so the halo that separates working from paused is a shadow rather than a keyframe; the four states were rendered with motion switched off to check they stay apart rather than assumed to.

- Gave the picture a way to be bigger, and said why the other one is greyed out. **Make it bigger** hides the rail, the panels and the heading and lets the picture use the height of the window as well as its width, computed from the window height and the picture's shape so the canvas and the box the pointer is placed in stay the same rectangle; the pointer lands on its coordinate in both views and both are asserted. It changes nothing about the session, so unlike **Screen size** it needs no pause, which is the answer to "let me see it properly" that does not move a coordinate out from under the assistant. **Screen size** still waits for a takeover, and now says so beside itself rather than being greyed out in silence.

- Rewrote the viewer for somebody who has never read this repository. Pause, resume and stop are Take over, Hand back and End session; running, paused and closed are Open, You are in control and Finished; "SbarOrbit: Fill field · working · Step 12" is "SbarOrbit is filling in a box right now · step 12"; a frame age in seconds is how old the picture is. A session that is running with nothing in flight now says Open rather than Working, because saying otherwise is the kind of small lie that teaches a person to stop believing the screen. The state itself moved to `data-state`, which is what a test should read, and the two browser tests now read it instead of the words. The short session identifier appears only when two cards would otherwise be identical, which is what it was for. The cost readout is kept and moved into a Technical details disclosure: it is the only instrument for what the viewer costs and it is not a sentence anybody needs first. The selects lost the platform's arrow and casing; the list one opens is still the browser's and cannot be styled, and a hand built listbox would have bought that one popup with a new keyboard model and a new accessibility tree.

- Wrote down what is wrong with the desktop panel's settings before changing any of it. Eighteen rows in seven groups answer seven questions: the glow is three settings, the notifications are two, and where the dot sits is four, one of which is already done by dragging the mark. The words are the project's rather than the reader's, the four state colours ask the reader to know Orbit has four states, nothing is ranked so a login switch reads like a transparency slider, and the window opens on a right click on a dot that is designed not to be noticed. The proposal is five decisions on one screen with the rest behind a disclosure, and it names the two parts that are not cheap: an eight place picker is a new widget that has to be operable from a keyboard, and collapsing two pairs of switches has to keep reading and writing both keys or an existing settings file loses a combination it holds. Nothing is built. [The study](docs/panel-redesign.md).

- The viewer opens as a window rather than a tab, and it is somewhere a person can stand. `preview open` and the desktop mark hand the link to a browser instead of printing it, in a window with no tab strip and no address bar, which is Chromium's `--app=` and nothing more: no manifest, no installation and no fixed port, because the link's port is allocated per broker and its access token lives in the fragment. An installed progressive web app is scoped to an origin, port included, and would be launched at `/` carrying no token, so it is a different change rather than a tidier version of this one. Browsers are found the way the desktop finds them, by the http scheme handler in a desktop entry, with the `WebBrowser` category required as well so an application that registers the scheme to catch its own sign-in links is not offered as somewhere to read the viewer. Flatpaks come out of the same scan, which matters on a desktop whose default browser is one. The Firefox family has no app window: site specific browsers were removed in Firefox 86 and no release build carries a replacement, so nothing is invented for them and they open a tab, which is why an unset choice passes over a default browser that cannot open a window and takes the first installed one that can. The family is read from the executable's name, and where that says nothing, from the private window action every browser ships and the two families spell differently, which is what identifies a Chromium build named Helium. Both choices are settings, `viewerBrowser` and `viewerAppWindow`, under **The viewer**. Spawning is not opening, so a browser that starts and rejects its arguments is given a moment for its exit code to arrive rather than being reported as a window that appeared; an immediate success is the forwarded second invocation and is treated as one. Tested against desktop entries written to a temporary tree, in the shapes this machine carries: a flatpak with file forwarding, a system Chromium, an Edge progressive web app, an unrelated launcher that handles http, and a browser the desktop hides. Then run for real, inside a private display rather than on the person's screen: `--app=` against a cold browser produced a window with no tab strip and no address bar, carrying the token fragment and connected; the same command against that running browser exited at once and a second chromeless window appeared beside the first. Desktop entries are Linux, so macOS and Windows still get a tab, and that is now written down rather than left implied.

- Rebuilt the viewer on a design system of its own, and made following the desktop a switch rather than the default. Every colour is a token declared once, elevation is five surfaces rather than five shadows because a shadow on a dark ground is nearly invisible, and the stage inverts the scale: it sits in a well below the page, inside a strong border and the deepest shadow, so the one thing that matters is cut into the ground and everything else is a step up from it. Sessions moved from a drop-down to a rail of cards, which is the whole of navigation: the drop-down showed one session at a time and said nothing about the rest, so a person running several agents could not see which one had stopped without opening it. The rail is built from `session.list` alone and holds no second live image, because every live image is a capture, an encode and a decode on each poll, and that cost is what the participant reported. Where the model is working now has three signals reading from one fact, an action in flight: the card's light breathes, the stage glows, and the activity line names actor, action, state and step; a running session with nothing in flight is open, not working, and none of them fires. `prefers-reduced-motion` removes all of it and each signal still reads as a colour. The agent's pointer dims over a stale frame instead of disappearing, because at the default one frame a second it had been invisible most of its life. The desktop's generated palette is now emitted under `:root[data-palette="desktop"]`, the attribute the switch writes, so Orbit's own palette is what every machine shows until somebody asks otherwise; an unscoped rule applied the desktop's colours to anyone who happened to have matugen configured. Rendered and measured in headless Chrome at 1440 and 390: no horizontal overflow at either width, the cost readout wraps inside its line, the pointer lands exactly on its canvas coordinate, no page errors. `tests/preview.test.ts` then passed against the rebuilt viewer, 3 tests and 25 assertions, which is authentication, live frames, pause, a click on the image, manual typing, the account snapshot, resume, stop, the bitmap accounting, the cost readout and both viewport widths. `tests/workspace-presence.test.ts` passed as well, 15 assertions, which is the agent and task names, the controlled page title, the pointer label and its position against the canvas to within two pixels, the handover of that label to the person after a manual click, the absence of typed text from the activity record, and the narrow width. Both needed several attempts to get a clear run: the shared slice sat at 487 to 512 of its 512 tasks and up to 1.86 GB of its 2 GB while other agents held live sessions, and a second Chrome cannot start in what is left. That is the contention the slice is designed to produce, and it is why the retry distinguished a missing CDP endpoint from a real failure. [viewer design](docs/viewer-design.md) is the reference.

- Made installation one command. `./install.sh` checks what the machine already has, prepares dependencies from the frozen lockfile with lifecycle scripts off, links `sbar-orbit` into `~/.local/bin`, installs and starts the broker service and the desktop mark, writes the agent connector configuration into Orbit's own directory, then asks the broker it just started for a `doctor` report. It is a driver over the pieces that already existed rather than a second implementation of any of them, and it runs inside the same shared budget as every other entry point. Each step is shown while it runs with its own elapsed time, a failure lands beside its step instead of in a wall of output afterwards, and the wait carries a rotating line naming a capability the project actually demonstrates. It installs nothing that needs root: Bun, Chrome and the Fedora capture tools stay the package manager's job, and each missing one is printed with the remedy the preflight already carries. The closing line says what the run did not check, because a linked command and a started service are installation state, not a measurement. `--dry-run` reports every step and writes nothing.

- Named the limit a contended run actually hits. With two other agents holding live sessions, the shared slice stood at 357 of 512 tasks and recorded `pids.events max 180`, so a spawn failed with `EAGAIN` and the account lease path reported its helper as unavailable, which sends a person looking for a program that is installed. That path now distinguishes a helper that cannot start from one that is not there, the way session creation already did. Reasoned from the captured failure: reproducing it means filling the budget other agents are using.

- Ran the unprivileged half of a fresh installation in a clean Fedora 44 container, at tier `Limited` with the limit printed beside it: no systemd user session, no cgroup delegation, no compositor, so the service, autostart, the private display and the resource budget were never exercised, and gate 3 is not closed by it. Tracked source is enough to install from, the frozen dependency install completes, and both the one command install and the launcher link refuse cleanly where the budget cannot exist. The run found two things a reading of the source would not have, and both are fixed: `preflight` reported systemd tools as available with no user manager behind them, and now looks for the private socket a running user manager keeps rather than only for the binary; and `doctor --report` read a Fedora container as this project's measured host class, and now recognises the markers a container runtime writes itself. The third finding is a gap that stays open, that the private compositor and pointer helper live in an untracked runtime directory a source release does not carry. Reproduce with `experiments/fresh-machine/run.sh`.

- Accepted `system` as the name for a private desktop session, with `fedora` still accepted for the same thing. A caller should not have to name a distribution to ask for a display of its own. It is vocabulary and not a wider claim: the backend still needs the wlroots runtime this project builds, and every reply, in `session.list`, observation and the journal, says `fedora`, so one thing keeps one name. `doctor` lists the aliases. Refreshed the project map, which still carried a test count from a day when there were 27, and which named a distribution where it meant a runtime.

- Stopped writing one system's package manager into instructions meant for any of them. A remedy for missing software now carries `packages`, the software itself, and a `command` built for whichever manager the machine actually has: dnf, apt, pacman, zypper or apk. A system with none of them gets the names and no command, because a wrong command is worse than none, and package names that differ between systems are already translated in the command. What is measured on one host class still says so: the tiers are about evidence, not instructions, and the one genuinely system bound prerequisite, the bootstrap that builds the private compositor, says in its own remedy that it pins Fedora packages and has been run nowhere else. The copy and paste prompt now starts from the published repository rather than assuming the source is already there, and `docs/packaging.md` no longer says this project has no remote, which stopped being true.

- Made the installation something an agent can do, without assuming a model, a host or a vendor. A remedy used to be an English sentence, so anything acting on the report had to read prose to decide whether it was allowed to run something. It is now a record: an id, a message, sometimes a command, and two separate flags. `needsElevation` describes the command, a package manager and a person. `agentMayRun` is the permission, and it is the one to branch on, because putting a directory on PATH needs no elevation and is still the person's shell configuration rather than an agent's to edit. `docs/agent-install.md` is the contract, and the copy and paste prompt in the README points at it rather than restating it, so the prompt cannot drift away from the installer. `tests/agent-contract.test.ts` runs the documented plan command and asserts the documented fields come back, and that every remedy and step the program can emit is named in the document. Tried twice by an agent given the prompt and the contract and nothing else: the first run installed correctly and found three defects, one boolean carrying two facts, a dry run naming the source instead of the prospective link, and a connector step that does not follow `--prefix` and did not say so; the second, after the fixes, reported it was never unsure what it was allowed to run. Two runs by one model on one host is the evidence there is, and it is not a claim that every agent can follow it. `AGENTS.md` is now tracked, with the build and test commands and the rule this project is judged on.

- Fixed a native supervisor that is killed rather than asked. A supervisor told to stop reaps its whole tree; one killed outright runs nothing, so its application kept running, became nobody's child and outlived the private runtime directory it had been given, with nothing reporting the loss. The backend now records the process group each supervisor leads, sweeps it when that supervisor exits and again when the session closes, and signals nothing whose own environment does not name this session's runtime directory, because a process identifier can be reused between the death and the sweep. The end to end check was run against the unfixed code first, where the application survived. This closes one failure mode of gate 2 and leaves account and application coverage where they were.

- Wrote the mint extension, and did not load it. `extension/` holds an MV3 extension that runs in the person's own browser, drives nothing, and mints narrow short lived origin scoped state for a separate Orbit browser through a native messaging host. Its decision path lives in modules a test can reach without a browser, which is what the tests cover: origin matching, cookie domain coverage, grant bounds, the envelopes and their refusal codes, and native messaging framing including every partial frame cut. G12 to G14 stay open, because each needs the extension loaded in a real browser and this project's rules keep agents out of the person's own browser.

- Gave the CDP handshake the same twenty seconds the socket before it already had. The two cover the same condition, a Chrome still starting on the shared budget while the host is busy, and they disagreed: the socket waited 20 s and the handshake immediately after it waited 10, so a starved host failed one line further down than the widened deadline was meant to reach. Caught from a captured run where `hyprctl` missed 37 of 84 samples and the 100 ms sample loop stalled for 3.7 seconds. That level of starvation could not be reproduced on demand, so the change is reasoned from the recorded failure rather than from a reproduction.

- Gave the project a mark, and put it where the project is seen: the panel's default shape, the quickshell bar indicator, the viewer header and its favicon, and the project map. It is two rounded squares offset on the diagonal, the screen you keep and the one Orbit opens beside it, with the outline stopping short at both crossings so they never touch. One flat silhouette in one colour, because the viewer's palette is regenerated from the wallpaper and the panel tints the mark by session state; the fixed blue is only for surfaces Orbit owns, and is the value the viewer already carried. Sized against real neighbours rather than guessed: in a bar row beside sixteen pixel glyphs 15 read lighter and 22 overflowed, so the indicator is 18. `brand/` holds the artwork and [brand](docs/brand.md) the rules. These are the first tracked binaries, and the publication audit refused them as it should; rather than bypass it, the audit now records the review it asks for as a path and the SHA-256 of its content, so replacing an approved file with different bytes fails again instead of inheriting the approval. See CONTRIBUTING.

- Removed a session's tmpfs runtime directory when it closes, and a private broker's socket directory when it stops. Nothing had, and on Fedora tmpfs pages stay charged to the cgroup that wrote them, so a day of sessions left 1.5 GB charged to the shared slice with nothing running and the kernel throttling everything under `MemoryHigh`. That throttling, not processor time, was behind a compositor that stopped answering, an input helper that missed its acknowledgement and screenshots that outlasted their deadline. Recorded in [resources](docs/resources.md).

- Removed the GitHub Actions workflow. It could run only the pure unit tests, never the suite that matters, and a partial green check reads as more than it is. Every gate runs locally and is documented in CONTRIBUTING.

- Applications in a private display now look the way they look on the desktop. The person's GTK, Qt and KDE theme settings, icon and cursor choices, colour schemes and fonts are carried into the session's private configuration, and nothing else: no desktop entries, MIME associations, recent files, dialog state or caches. `kdeglobals` is filtered to its appearance groups. Fonts are named by their real path in a fontconfig fragment because a copy or a link would make every session rescan every installed font, which on this workstation held the compositor for 3.9 seconds and stopped sessions starting at all. See [appearance](docs/appearance.md).

- Added a quickshell bar module, `desktop/quickshell/`: a capsule in the person's own bar drawn in the shell's own colours, showing the state by colour, blinking when a session or a window appears, listing the sessions on hover and opening the viewer on click, and taking no width at all when nothing is running. It reads a new helper, `desktop/orbit-stream.py`, which prints one JSON line per change at 18 MB resident and 0.04% of one core, where `sbar-orbit status --watch` answers a related question at 117 MB and 1.3%. The broker client both share is `desktop/orbit_client.py`.
- The panel mark is a capsule, five pixels wide with no number on it, and the card list it opens on hover no longer strands itself open. The pointer is read from the controller property rather than from a pair of crossing signals, with a delay each way and a watchdog behind them. Removed the tooltip: it asked GTK to export a toplevel, which a layer surface is not, and the protocol error killed the panel the moment the pointer rested on it. The surface is pinned to one end of its edge, so the capsule no longer slides out from under the pointer as the cards open. Settings gained the offline colour, the distance from the edge, hiding the mark while nothing runs, a reset, Escape, scrolling and screen-reader names, and they are written once the hand stops moving and renamed into place rather than truncated. Cut the panel's own cost: one worker for its life instead of a thread a second, one kept connection instead of two a second, and presence only when something is going to read it.
- Found what actually freezes this desktop, and it is not Orbit: of 868 recorded stalls only 18 were real freezes, all of them in early September, all of them with swap completely full and two agent processes holding 20 GB between them. Recorded the rest in [resources](docs/resources.md), including the shell idiom that burns a whole core while pretending to sleep.
- Redesigned the panel around a small dot that says the state by colour, no number by default, blinking once when a session or an application appears and expanding on hover into a card per session. Added a settings window a right click opens: the edge and monitor, the shape (dot or dot with a count), the size, a colour for each state, the working frame and its colour, notifications and the blink, each saved and applied at once. Measured the panel's cost at 0.1% of one core and the broker's polling at 0.5%, and documented the real causes of the processor stalls (a leftover heavy session in the throttled slice, host-load starvation, a runaway from another agent), none of them the panel. Settings persist in `~/.config/sbar-orbit/panel.json`; the mark lands on the largest monitor unless one is named.
- Added the working indicator, `sbar-orbit panel --indicator`: four click-through overlay strips framing the monitor while any session's action is in flight, mapped once per state change and painted above the person's own `gtk.css`. Verified inside a private display with edge pixels sampled during and after a slow launch. The panel now takes the person's GTK colour names when their theme defines them.
- Profiles are removed when a session stops and a broker's workspace when it closes; `sbar-orbit clean` removes workspaces whose broker is gone, keeping a live broker's directory when its recorded socket still answers. Before this every test and experiment profile was retained forever, 4.5 GB on this workstation.
- Added `sbar-orbit status`, a read-only view of every session with tabs, windows, activity and pointer, a `--watch` mode that prints only on change, and `session.presence`, which is `observe` without the frame. Added `sbar-orbit panel`, a wlr-layer-shell edge strip that shows session and tab counts, expands on hover into one row per session and opens the viewer on click; it runs outside the shared budget as the person's own desktop process, uses the Cairo renderer, polls off its main thread and opens only a loopback viewer link. Captured inside a private display by `experiments/panel-check.ts`.

- Started backends one at a time, with a 30 second queue deadline, and widened the browser action timeout to 5 seconds, navigation to 15 and the Chrome connection wait to 20. `experiments/concurrent-sessions.ts` drives several sessions from independent loops on one broker: three Chromes and two compositors booting together on one core had timed each other out, and a locator that resolves in 200 ms alone took over three seconds with four other sessions working, which was being reported as a missing element. With the fixes, three browser sessions and two private displays complete 20 of 20 rounds in 9.8 seconds at 93 percent of one core.

- Kept each private display's compositor output in `compositor.log` beside the session, bounded, because it is the only evidence when a display fails to start.

- Raised the native launch deadline to thirty seconds for Electron applications on a software-rendered display. Docker Desktop still does not open there: its launcher requires the session bus, which a private display deliberately lacks, and the Electron binary underneath did not map a window in that time.

- Closed the secret-scanning gap. Gitleaks now runs, the whole history scanned clean across 28 commits, and the pre-commit gate was verified by staging a fabricated key and watching it refuse. Recorded what the publication audit and Gitleaks each catch, since neither sees what the other does. Also brought `scripts` under `tsconfig.json`, which needed no code changes, and added the theme and surface unit tests to continuous integration.

- Added `open-tab`, so an agent can open a tab itself instead of only following one a site opened. A trial with a local agent host found the gap: the host tried to open a second tab, could not, and reported that Orbit did not support it.

- Rewrote the MCP tool descriptions to lead with what each tool is for and to name every action it can perform, rather than opening with constraints, and described the session labelling fields so an agent names itself in the viewer. What decided whether a host reached for Orbit at all was the wording of the request, not the descriptions; the trial is recorded in [connectors](docs/connectors.md).

- Made the session surface a session property instead of a fixed 1280 by 800. A session can be created at a size and resized while it runs, on both backends: browser sessions resize every open tab so a tab switch does not change what a coordinate means, and native sessions resize the private display itself. Native sessions also gained `window` with fullscreen, restore, focus and close, which gives one application the whole display at no per-frame cost. Coordinate bounds, which were hardcoded in three places, now follow the session's actual surface. The cap is 1,920 by 1,200 total pixels, taken from `experiments/surface-cost.ts`: capture latency is nearly flat across that range, but continuous capture on a private display already reaches a whole core at the cap.

- Gave the viewer a tab strip and a size control. The strip lists browser tabs, or the windows of a private display, numbered the way observation reports them, and selecting one follows that tab or focuses that window. Both, like typing, require pause first. Observation now reports a `tabs` list and the real surface size on both backends.

- Let the viewer take the desktop's colour scheme. It paints from Material 3 colour role tokens with the shipped palette as fallback, and the broker serves a generated `/theme.css` read from `ORBIT_THEME`, an Orbit theme file, or a matugen or quickshell palette. Values are checked against a fixed key list before they reach CSS, because the viewer origin holds a session access token. With no theme file present nothing changes. See [theming](docs/theming.md).

- Gave native sessions private XDG base directories, so an application cannot restore the person's own previous session or recent documents into the agent's workspace. A trial had shown GNOME Text Editor doing exactly that. Added `experiments/multi-application.ts`, which opens four real applications at once, edits a real file through one of them and reads the result back from disk.

- Added an optional managed broker: `sbar-orbit service install` writes a `systemd --user` service bound to a slice that carries the shared budget, and `serve --managed-socket` binds one fixed socket at `$XDG_RUNTIME_DIR/sbar-orbit/broker.sock`. A second managed broker refuses rather than displacing the first; a socket nothing answers on is treated as stale. Generated MCP configuration now prefers that socket, so it survives a restart. Enabling, starting and stopping stay with `systemctl`. Sessions still do not survive a restart. Brokers started by tests and experiments keep their own private sockets and are unaffected.

- Corrected the capability list `doctor` reports, which still omitted scroll and the tab actions.

- Added `experiments/live-trial.ts` and `experiments/pointer-separation.ts`, recording measured session cost and evidence that a session holds no connection to the host display. Corrected the MCP observe description, the preview limits and the project milestones, which still described one page per session, PNG capture and an unmoved resource gate.

- Followed browser tabs that a site opens by itself, so a login, consent or payment window is now reachable instead of leaving the agent bound to the opening tab for the life of the session. Added `select-tab` and `close-tab` actions keyed to the 1-based number observation reports, sized adopted tabs to the session viewport so reported and captured dimensions agree, moved the pointer overlay to whichever tab is followed, and fell back to a surviving tab when the followed one closes. Each action and each frame now resolves the followed tab once, so a tab opened mid-action cannot redirect it.

- Captured frames as JPEG quality 80 instead of PNG on both backends. Measured on one Fedora host: browser capture fell from 68 ms and 188 KiB per frame to 47 ms and 70 KiB, and native capture fell from 77.8 ms to 8.1 ms because PNG deflate, not the per-frame `grim` process or the sway tree query, was the whole cost. Frames now declare their format in `mimeType`, which the MCP adapter accepts for both formats.

- Stopped the viewer scheduling its next poll with no delay when an iteration outlasted its cadence. A slow desktop previously polled back to back at a full duty cycle, including in the default 1 FPS mode. The viewer now idles at least as long as the iteration cost, degrading its frame rate instead, and reports its own measured per-frame cost and busy share.

- Enforced source-manifest content hashes during extracted-release repackaging, rejecting invalid paths, linked inputs and version mismatches before archive creation. Added a standalone content verifier.

- Added standalone `sbar-orbit preflight` to report browser/native prerequisite availability without launching applications or requiring a broker socket.

- Added local source launcher activation, atomic upgrade/rollback and link-only uninstall. Retained source and account/workspace data are preserved; dependencies remain a separate preparation step.

- Stopped hidden preview tabs from decoding or drawing late-arriving frames, and skipped their freshness updates. Already-started capture/decoding can finish; bitmaps are still released.

- Added bounded read-only CPU observation, separating Orbit one-core and machine percentages from host busy/iowait totals. This does not establish the cause of the participant CPU spike.

- Fixed publication-path validation so example environment files cannot bypass private-directory exclusions; index auditing and packaging now share one policy.

- Defaulted preview to 1 FPS, added on-demand and opt-in Smooth modes, stopped hidden-tab polling and added reconnect backoff. Scheduling tests pass; desktop CPU acceptance remains unresolved.

- Added named workspaces, current page/application identity, action status and a labelled pointer overlay for following agent work and manual takeover.

- Added browser vertical wheel actions and paused viewer scrolling, with shared validation across both backends.

- Added paused native-session wheel control in the viewer, with scaled coordinates and no gesture backlog.

- Added native vertical wheel actions through broker, CLI and MCP.
- Fixed the first GTK XInput2 scroll delta being consumed as initialization by sending a zero axis baseline; no core-input override is required in the tested fixture.

- Strengthened native crash checks to inspect all task threads and require zombie reaping.
- Repeated browser/native recovery and real-editor file reservation suites three times on Fedora.
- Human takeover and broader application compatibility remain under validation.

## 0.1.0-alpha.1 - 2026-09-10

Initial public alpha, following private local prototypes.

### Added

- Local broker, CLI and MCP adapter for tool-capable agent hosts.
- Owned headless browser sessions with separate profiles and ordered actions.
- Optional Canvas viewer with pause, scoped manual input, resume and stop.
- Fedora private Wayland/Xwayland display backend and Unicode paste.
- Named account snapshots and cooperative selected-file reservations.
- Shared Linux CPU, memory, swap and task limits; process-tree cleanup.
- Source packaging, privacy checks, test fixtures and concise architecture documentation.
- Experimental read-only Hyprland focus telemetry and human takeover trial.

### Validated

- Scripted 10-minute browser/viewer run at approximately 5 FPS.
- Browser and native lifecycle, clipboard and file-reservation tests.
- Claude Code and Codex model-host browser/native examples in local trials.

### Limitations

- Fedora/Linux alpha; macOS and Windows adapters remain unverified.
- Display separation is not a security sandbox.
- Human trial confirmation, broad account compatibility and clean-machine installation remain open.
